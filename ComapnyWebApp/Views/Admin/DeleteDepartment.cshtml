@model ComapnyWebApp.Models.Department


@{
    ViewData["Title"] = "Delete Department";
    Layout = "~/Views/Shared/Admin_Layout.cshtml";
}

@* <div class="modal fade" id="DelDepartmentModal" tabindex="-1" aria-labelledby="DelDepartmentModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="DelDepartmentModalLabel">Delete Department</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body"> *@
                <h1>Delete Department</h1>
                <p class="text-danger">@ViewData["ErrorMessage"]</p>
                <h3>Are you sure you want to delete this Department?</h3>
                <div>
                    <h4>Department details:</h4>
                    <hr />
                    <dl class="row">
                        <dt>
                            @Html.DisplayNameFor(model => model.DeptName)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.DeptName)
                        </dd>
                    </dl>

                    <form asp-action="CDeleteDepartment" asp-route-id="@Model.Id" method="post">
                        <div class="form-group">
                            <input type="submit" value="Delete" class="btn btn-danger" />
                            <a asp-action="Department">Back to List</a>
                        </div>
                    </form>
                </div>
           @*  </div>
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {
        $('#DelDepartmentModal').modal('show');
    });
</script> *@
